name: widgets_app
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.2 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  go_router: ^7.1.1

dev_dependencies:
  animate_do: ^3.0.2
  flutter_lints: ^2.0.0
  flutter_riverpod: ^2.3.6
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/images/
